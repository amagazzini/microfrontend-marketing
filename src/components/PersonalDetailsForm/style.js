const detailContainer = {
    display: "flex",
    flexWrap: "wrap",
    justifyContent: "space-between"
} 

const input = {
    padding: "1rem",
    marginBottom: "1rem"
}

const fullWidth = {
    width: "100%"
}

const halfWidth = {
    width: "40%"
}

const PersonalDetailsFormStyle = {
    detailContainer,
    input,
    fullWidth,
    halfWidth
}

export default PersonalDetailsFormStyle;