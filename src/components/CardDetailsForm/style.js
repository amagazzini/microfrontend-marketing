const form = {
    width: "400px",
    display: "flex",
    justifyContent: "center",
    flexDirection: "column"
}

const cardContainer = {
    lineHeight: "4rem",
    marginBottom: "2rem",
    padding: "0.5rem 2rem 2rem 2rem",
    borderRadius: "1%/3%",
    backgroundColor: "white"
}

const CardDetailFormStyle = {
    form,
    cardContainer,
}

export default CardDetailFormStyle;